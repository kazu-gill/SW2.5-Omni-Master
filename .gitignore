# ── ローカル設定（絶対パス・秘匿情報を含む） ────────────────────────────────
config.env
*mock*

# ── ランタイム状態 ────────────────────────────────────────────────────────────
.pids
logs/*
!logs/.gitkeep
*.log

# ── データベース ──────────────────────────────────────────────────────────────
data/omni.db
data/omni.db-shm
data/omni.db-wal

# ── LLM モデルファイル（巨大バイナリ） ───────────────────────────────────────
*.gguf
*.safetensors
*.ckpt
*.pt
*.pth
data/models/

# ── 生成画像（バッチ生成結果） ────────────────────────────────────────────────
data/images/*
!data/images/.gitkeep

# ── ルールブック PDF（著作物・大容量） ───────────────────────────────────────
data/rulebook/*.pdf
data/rulebook/*.PDF
# Markdown 変換版は追跡対象（!data/rulebook/*.md は不要、デフォルトで含まれる）

# ── コンパイル済みバイナリ ────────────────────────────────────────────────────
.bin/
orchestrator/server

# ── Node.js ───────────────────────────────────────────────────────────────────
ui/node_modules/
ui/dist/
ui/dist-ssr/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
ai-agents/.venv/

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.suo
*.sw?

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
._*

# ── Claude CLI プロジェクトキャッシュ ─────────────────────────────────────────
.claude/

# ── 大容量 PDF 仕様書・テストレポート ────────────────────────────────────────
*.pdf
*.PDF
